# ── Этап 1: сборка статики Vite ──
FROM node:22-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
# Адрес API задаётся на этапе сборки и «впекается» в бандл.
# По умолчанию /api — относительный путь, его проксирует nginx на бэкенд.
ARG VITE_API_URL=/api
ENV VITE_API_URL=$VITE_API_URL
RUN npm run build

# ── Этап 2: отдаём собранную статику через nginx + проксируем /api ──
FROM nginx:alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 80
